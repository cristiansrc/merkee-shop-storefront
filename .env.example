# Configuración del storefront merkee.shop

# URL base de la API (sin trailing slash)
VITE_API_BASE_URL=https://api.merkee.shop/v1

# Usar mocks cuando la API no esté disponible
# Establecer a "true" para forzar uso de mocks
VITE_USE_MOCKS=false
